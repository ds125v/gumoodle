language: php
php:
  - "5.4"
  - "5.3"
env:
 - DB=mysqli
 - DB=pgsql
before_script:
 - pear channel-discover pear.phpunit.de
 - pear channel-discover pear.symfony.com
 - pear install pear.phpunit.de/DbUnit
 - phpenv rehash
 - cp config-dist.php config.php
 - echo "$CFG->dbuser='root';$CFG->dbpassword='';" >> config.php
 - echo "$CFG->wwwroot='http://localhost/moodle';$CFG->dataroot='/home/travis/moodledata';" >> config.php
 - echo "$CFG->phpunit_prefix='phpu_';$CFG->phpunit_dataroot='/home/travis/phpu_moodledata';" >> config.php
 - sh -c "if [ '$DB' = 'mysqli' ]; then mysql -e 'create database moodle default character set UTF8 collate UTF8_bin;'; fi"
 - sh -c "if [ '$DB' = 'pgsql' ]; then psql -c 'create database moodle;' -U postgres; fi"
 - sh -c "if [ '$DB' = 'pgsql' ]; then echo \"$CFG->dbtype='pgsql';$CFG->dbuser='postgres';\" >> config.php; fi"
 - mkdir -m777 $HOME/moodledata
 - php admin/tool/phpunit/cli/init.php
